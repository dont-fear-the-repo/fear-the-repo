import { createConstants } from '../utils';

export default createConstants(
  'SAVE_RESUME',
  'DROP_BULLET',
  'MOVE_BLOCK',
  'MOVE_BULLET',
  'UPDATE_RESUME_WITH_SERVER_RESPONSE',
  'UPDATE_LOCAL_STATE'
);
